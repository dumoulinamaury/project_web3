
						<nav>
							<ul class="header-nav-primary nav nav-pills collapse navbar-collapse">
							{% for d in nav %}
								{% if d['access'] == "public" %}
									{% set class1 = '' %}
									{% if d['pages'][0]['pages'] is defined %} {% set class1 = 'has-mega-menu ' %} {% endif %}
									{% set class2 = '' %}
									{% if idActive == d['id'] %} {% set class2 = 'active' %} {% endif %} 
								<li {% if (class1 or class2) is not empty %}class="{{class1}}{{class2}}"{% endif %}>
									<a href="#">{{ d['name'] }} <i class="fa fa-chevron-down"></i></a>
										
									{% if d['pages'][0]['pages'] is defined %} 
									<ul class="mega-menu">
										{% for submenu in d['pages'] %} 
										<li>
											<a href="#">{{ submenu['name'] }}</a>
											<ul>
												{% for page in submenu['pages'] %}<li><a href="{{ path(page['route'], {'id' : 0} ) }}">{{ page['name'] }}</a></li>
												{% endfor %} 
											</ul>
										</li>
										{% endfor %} 
									</ul>
									{% else %}
									<ul class="sub-menu">	
										{% for page in d['pages'] %}<li><a href="{{ path(page['route'], {'id' : 0} ) }}">{{ page['name'] }}</a></li>
										{% endfor %} 
									</ul>
									{% endif %} 
								</li>
								{% endif %}
							{% endfor %} 
							</ul>
						</nav>
